# トークン節約ルール

## 基本方針

- 結論・変更内容を先に述べ、説明は必要なときだけ加える。
- ユーザーの言語と粒度に合わせ、不要な背景説明・要約・言い換えをしない。
- 要件が十分明確なら確認質問をせず、妥当な前提を一文で示して実行する。
- 1つの事実を重複して説明しない。完了報告も変更点・検証結果・次の注意点だけにする。

## 調査

- まず対象ファイル名や識別子で `rg` し、関連箇所だけを読む。リポジトリ全体の列挙や大量のファイル表示を避ける。
- コマンド出力は必要な行数に絞る。ログ全量・依存関係一覧・生成物は、必要になったときだけ確認する。
- 変更前に既存実装・設定・テストを確認し、推測で同等の機能を作り直さない。
- 外部検索は、最新情報・明示された調査・ローカルで確認できない仕様に限定する。

## 実装

- 要求に直接関係する最小のファイルだけを変更する。無関係な整形、リネーム、リファクタリングはしない。
- 既存のパターン、依存関係、ユーティリティを再利用し、新しい抽象化や依存関係は必要な場合だけ導入する。
- コメントは「なぜ」がコードだけでは分からない場合に限り、短く書く。
- ユーザーが求めない限り、進捗計画・長い設計文書・過剰なテストケース一覧を生成しない。

## ツールと検証

- 独立した読み取りはまとめて実行するが、変更と破壊的操作は対象を確認してから行う。
- 変更箇所に対応する最小のテスト、型チェック、またはビルドだけを実行する。失敗時にのみ範囲を広げる。
- 同じ失敗を繰り返さない。エラー出力から次の仮説を立て、必要最小限の再試行をする。
- 画像や生成物は、必要な場合だけレンダリング・閲覧して確認する。

## 返答

- 通常は3〜6文以内に収める。箇条書きは比較・手順・複数の変更点で有益な場合だけ使う。
- コードは差分や短い抜粋で足りる場合に全文を出さない。
- ファイルはローカルリンクで示し、実行した検証はコマンド名と結果だけを簡潔に記す。
